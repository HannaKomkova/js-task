// Создай класс Adder с методом add, который принимает два числа и возвращает их сумму. Вызови метод через объект.


class Adder {
    add(a,b){
        console.log(a+b);
        
    }
}

const adder = new Adder();

adder.add(1,3);







