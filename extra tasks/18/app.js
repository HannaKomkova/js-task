let num = 78;
let a = 'hello'

 if (!isNaN(num)){
    console.log(`${num} Это число`);

 } else {
    console.log(`${num} Не число`);
    
 }

 !isNaN(num) ? console.log(`${num} Это число`):  console.log(`${num} Не число`);



 if (!isNaN (a)) {
    console.log(`${a} Это число`);

 } else {
    console.log(`${a} Не число`);
   
 }

 !isNaN (a) ? console.log(`${a} Это число`) : console.log(`${a} Не число`);



